<Window x:Class="Librya.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Librya"
        mc:Ignorable="d"
          Title="Учет книг в библиотеке" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,15,5">
                <TextBlock Text="Поиск (Название/Автор):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="TxtSearch" Width="150" Height="22" TextChanged="TxtSearch_TextChanged"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,15,5">
                <TextBlock Text="Жанр:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="CmbGenre" Width="120" Height="22" SelectionChanged="CmbGenre_SelectionChanged"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,15,5">
                <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="CmbSort" Width="140" Height="22" SelectionChanged="CmbSort_SelectionChanged"/>
            </StackPanel>
        </WrapPanel>

        <DataGrid x:Name="DataGridBooks" Grid.Row="1" 
              AutoGenerateColumns="False" 
              IsReadOnly="True" 
              CanUserAddRows="False" 
              AlternatingRowBackground="#F2F2F2" 
              GridLinesVisibility="Horizontal" SelectionChanged="DataGridBooks_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название книги" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Автор" Binding="{Binding Author}" Width="1.5*"/>
                <DataGridTextColumn Header="Жанр" Binding="{Binding Genres.Name}" Width="1*"/>
                <DataGridTextColumn Header="Год издания" Binding="{Binding YearOfPublication}" Width="1*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
